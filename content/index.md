---
title: Курс C++ МФТИ
type: map
status: read
tags: [cpp, map]
created: 2026-09-12
---

# Курс C++ МФТИ

Точка входа в базу. Отсюда — ко всем лекциям и темам курса.

## Как этим пользоваться

1. Материалы новой лекции кладёшь в `90-Входящие/`.
2. Запускаешь `/lecture` — ассистент читает материалы и показывает план конспекта
   и список терминов.
3. Правишь план, подтверждаешь — конспект и заметки терминов появляются в базе
   со статусом `draft`.
4. Читаешь, дополняешь, заполняешь `## Своими словами` и `## Вопросы`,
   поднимаешь `status` до `read`, позже до `mastered`.

## Лекции

| № | Лекция | Дата | Статус |
|---|--------|------|--------|
| 01 | [[L01 - Типы данных, переменные и ввод-вывод]] | 12.09.2026 | draft |

## Термины по темам

**Типы данных**
- [[Целочисленные типы]] — размеры, диапазоны, суффиксы литералов
- [[Беззнаковые типы]] — обёртывание и ловушки сравнения
- [[Типы с плавающей точкой]] — точность и почему нельзя сравнивать через `==`

**Операции и преобразования**
- [[Целочисленное деление и остаток]] — усечение, отрицательные, округление вверх
- [[static_cast]] — явное приведение типов
- [[const и constexpr]] — именованные константы
- [[Инициализация переменных]] — почему `int x = 0;`

**Ввод-вывод**
- [[Потоки ввода и вывода]] — `std::cin`, `std::cout`, `'\n'` против `std::endl`
- [[Форматирование вывода]] — `<iomanip>`, липкие и одноразовые манипуляторы

**Язык и инструменты**
- [[Компиляция и сборка]] — компиляторы, стадии сборки, флаги
- [[Подключение заголовков]] — как работает `#include`
- [[Неопределённое поведение]] — UB и как его ловить
- [[Кодстайл курса]] — правила оформления кода

Полный список — папка `20-Термины/`. Фильтр по тегам работает через поиск
Obsidian: `path:20-Термины tag:#cpp`.

## Статусы

- `draft` — написано ассистентом, мной не проверено.
- `read` — прочитал, согласен, что верно.
- `mastered` — могу объяснить без подглядывания.

Найти всё непроверенное: поиск по базе `[status:draft]`.
Термины одной темы: `path:20-Термины tag:#cpp/memory`.

## Структура базы

- `10-Лекции/` — конспекты лекций.
- `20-Термины/` — атомарные заметки на сквозные понятия.
- `30-Код/` — исходники примеров с лекций и разборы.
- `90-Входящие/` — сырьё, которое ещё не разобрано.
- `99-Шаблоны/` — шаблоны заметок.
